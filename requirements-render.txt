# Core requirements optimized for Render.com deployment
# Lighter versions and Render-specific optimizations

# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.2
aiohttp>=3.9.0

# Database (PostgreSQL only for Render)
sqlalchemy>=2.0.23
alembic>=1.13.1
psycopg2-binary>=2.9.9

# Data processing (lighter versions)
pandas>=2.1.4
numpy>=1.24.3
python-dateutil>=2.8.2

# Visualization (essential only)
matplotlib>=3.8.2
plotly>=5.17.0

# Web framework for dashboard
flask>=3.0.0
gunicorn>=21.2.0

# Email and reporting
jinja2>=3.1.2

# Configuration and environment
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Scheduling
schedule>=1.2.0

# Web scraping utilities
fake-useragent>=1.4.0
lxml>=4.9.3

# CLI and logging
click>=8.1.7
loguru>=0.7.2

# Essential only (no dev dependencies for production)
# No pytest, black, etc. to keep image smaller